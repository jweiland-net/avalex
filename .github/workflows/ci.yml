# Adapted from https://github.com/TYPO3GmbH/blog/blob/master/.github/workflows/ci.yml
name: CI

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        typo3: [^9.5, ^10.4]
        php: ['7.3', '7.4']
        package: [typo3/cms-core]
        include:
          - typo3: ^6.2
            php: '5.6'
            composer: '1'
            package: typo3/cms
          - typo3: ^7.6
            php: '5.6'
            composer: '1'
            package: typo3/cms
          - typo3: ^7.6
            php: '7.0'
            composer: '1'
            package: typo3/cms
          - typo3: ^7.6
            php: '7.1'
            composer: '1'
            package: typo3/cms
          - typo3: ^8.7
            php: '7.0'
            composer: '1'
            package: typo3/cms-core
          - typo3: ^8.7
            php: '7.1'
            composer: '1'
            package: typo3/cms-core
          - typo3: ^8.7
            php: '7.2'
            composer: '1'
            package: typo3/cms-core
          - typo3: ^11.0
            php: '7.4'
            composer: '2'
            package: typo3/cms-core
          - typo3: ^11.0
            php: '8.0'
            composer: '2'
            package: typo3/cms-core
            # Do not test against PHP 8.1. php-cs-fixer 2.19 does not work with PHP 8.1
    services:
      mysql:
        image: mysql:5.6
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: false
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: test
        ports:
          - 3306/tcp
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up PHP version ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}

      - name: Use correct composer version for specific TYPO3 version
        run: |
          composer self-update --${{ matrix.composer }}

      - name: Environment Check
        run: |
          php --version
          composer --version

      - name: Validate composer.json and composer.lock
        run: composer validate

      - name: Install dependencies with ${{ matrix.package }}:${{ matrix.typo3 }}
        run: |
          composer require ${{ matrix.package }}:${{ matrix.typo3 }} --no-progress
          git checkout composer.json

      - name: php-cs-fixer
        run: composer ci:php:fixer

      - name: Lint PHP
        run: composer ci:php:lint

      - name: Functional Tests
        if: matrix.php > 7.0
        env:
          typo3DatabaseName: test
          typo3DatabaseUsername: root
          typo3DatabasePassword: password
          typo3DatabaseHost: 127.0.0.1
          typo3DatabasePort: ${{ job.services.mysql.ports['3306'] }}
        run: |
          composer ci:tests:functional
